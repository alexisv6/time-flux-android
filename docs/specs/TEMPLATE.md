# Spec NNN — <Feature name>

**Status:** draft | approved | shipped | abandoned
**Created:** YYYY-MM-DD
**Plan:** `docs/plans/NNN-<slug>.md`

---

## Problem

What's wrong or missing today, in terms of the user's experience — not the code's. Two or three
sentences. If the honest answer is "nothing is wrong, this is just next on the list", say that and
say why it's next.

## Why now

What this unblocks, or what it costs to keep deferring it.

## User-visible behaviour

Describe what the user can do when this ships, in prose or a short walkthrough. Someone should be
able to read this and picture the screens without seeing a design. No class names, no file paths.

## Non-goals

Explicit list of things a reasonable reader might assume are included and are **not**. This is the
most valuable section in the document — be generous with it.

## Decisions

Each decision with its rationale. These are the things that would otherwise be re-litigated
mid-implementation.

**Decision: <the call>**
Rationale: <why, including what was rejected>

## Constraints

Which of the ten data-model principles apply, which product decisions bind (v1 local-only, media
format, module priority), and any platform constraints.

## Acceptance criteria

Given/When/Then, one per behaviour the spec promises. These become the plan's verification steps.

- **Given** … **When** … **Then** …

## Open questions

Numbered, each with the options considered and the cost of getting it wrong. The spec is not
approved while any of these is unanswered — close it or explicitly defer it with a note.

1. …
